language: node_js
node_js:
  - "12.13.1"
dist: trusty
sudo: required
branches:
  only:
  - main
before_script:
  - npm install -g @angular/cli
script:
  - ng lint
  # - npm run test-headless
  - ng build --prod


deploy:
  edge: true
  provider: elasticbeanstalk
  access_key_id:
    secure: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
  region: ap-southeast-1
  app: test-app
  env: TestApp-env
  bucket_name:  elasticbeanstalk-ap-southeast-1-722632012472
  bucket_path: docker-multi
  on:
    branch: main